great now we still need to prompt the user for desired containers.  We need to sniff out all passwords on-the-fly from any given container loadout in the .yml which is created as the user indicates is wanted from the standard, for example, "docker pull wordpress" or any other docker pull which might arise in library https://hub.docker.com/search?  The user will be prompted one at a time for which containers to add the the .yml until the user is satisfied